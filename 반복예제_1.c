#include<stdio.h>

int main(void)
{
	/*
	int sh = 1;
	while(sh <= 10)
	{
		printf("#");
		sh++;
	}
	//-----------------------------
	int num = 1;
	while(num < 6)
	{
		printf("반복내용 : %d\n",num);
		num++;
	}
	printf("반복문종료 후 num = %d\n",num);
	//------------------------------------
	int i = 30;
	while(i >= 10)
	{
		printf("%d ",i);
		i-=2;
	}
	//-----------------------------
	int sum = 0, i = 1;
	while(i <= 10)
	{
		sum = i + sum; //sum+=i;
		i++;
	}
	printf("1부터 10까지의 합 : %d",sum);
	*/
	
	
}
